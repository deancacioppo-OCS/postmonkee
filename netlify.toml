[build]
  # Build command
  command = "npm install && npm run build"
  
  # Publish directory
  publish = "dist"

[build.environment]
  # Your backend API URL
  VITE_API_URL = "https://blm-rmbc.onrender.com"

# Redirect all routes to index.html for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
